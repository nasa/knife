
# WARNING this is a developer makefile, not generated by Autotools.

test: all
	( cd ../cases/tiny3d && \
	  time ../../`uname`/src/knife -s cylinder.fgrid -b 1 -b 2 \
	                               -v gbumpn.fgrid )
	( cd ../cases/big3d && \
	  time ../../`uname`/src/knife -s cylinder.fgrid -b 1 -b 2 \
	                               -v gbumpn.fgrid  )
	( cd ../cases/om6box && \
	time ../../`uname`/src/knife -s om6.fgrid -b 1 -b 2 \
	                             -v om6box.fgrid )

all:
	( cd ../`uname` && $(MAKE) all )

check:
	( cd ../`uname` && $(MAKE) check )

install:
	( cd ../`uname` && $(MAKE) install )

clean:
	rm -rf Adj Primal Near Node

bu:
	( cd .. && $(MAKE) bu )

