
# WARNING this is a developer makefile, not generated by Autotools.

all:
	( cd ../`uname` && $(MAKE) all )

check:
	( cd ../`uname` && $(MAKE) check )

install:
	( cd ../`uname` && $(MAKE) install )

clean:
	rm -rf Adj

bu:
	( cd .. && $(MAKE) bu )
